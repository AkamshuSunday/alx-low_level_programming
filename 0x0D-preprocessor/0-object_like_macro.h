#ifndef OBJECT_LIKE_MACRO_H
#define OBJECT_LIKE_MACRO_H

/* 
   The following code uses an include guard to prevent multiple inclusions
   of this header file. If OBJECT_LIKE_MACRO_H has not been defined yet,
   it gets defined here.
*/

#define SIZE 1024

/* 
   The macro SIZE is defined with the value 1024. Macros work like symbolic
   constants, where every instance of SIZE in your code will be replaced
   with 1024 during preprocessing.
*/

#endif

/* 
   This marks the end of the header file. 
   The #endif corresponds to the #ifndef at the beginning,
   effectively closing the include guard.
*/

