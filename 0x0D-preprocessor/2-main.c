#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This function prints the name of the file and returns 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Print the name of the file followed by a newline */
	printf("%s\n", __FILE__);

	/* Return 0 to indicate successful execution */
	return (0);
}

