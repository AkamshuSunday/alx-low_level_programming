#include "main.h"

/**
 * _isdigit - Checks if a character is a digit (0 through 9).
 * * b: The integer value to be checked.
 * * return: 1 if b is a digit, 0 otherwise.
 */
int _isdigit(int b)
{
	return (b >= '0' && b <= '9');
}
